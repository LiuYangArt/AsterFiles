# AsterFiles 前期架构实施计划

## 目标

在继续扩展界面和文件操作前，先固定三条不会反复推倒的边界：

1. Windows 原始路径不会因 UTF-8 展示转换而丢失；
2. 导航和后台加载天然属于某个标签页，未来增加多标签不重写核心；
3. 本地磁盘、UNC 和未来网络位置再慢，也不能在 UI 线程执行探测或读取。

具体 FTP、SFTP、WebDAV 等协议继续后置。本阶段只建设实际需要的本地浏览闭环，不提前搭建通用网络框架。

## 必须长期保持的边界

- 文件和目录身份留在 Rust：使用 `PathBuf/OsString` 或稳定 ID。Slint 只接收展示文本和稳定 ID。
- UI 只提交意图，不调用 `is_dir`、`exists`、`read_dir`、Shell、COM 或网络 API。
- 每次加载携带 `TabId + RequestId`。结果只可写回对应标签页的最新请求。
- 新导航、关闭标签页和退出应用都要能取消旧任务；取消后迟到结果必须丢弃。
- 目录读取分批提交，优先让首批可见项目出现；完成前 UI 仍可导航和切换标签。
- 标签页各自保存路径、历史、列表、选择、排序、加载状态和错误；文件任务中心、缓存和受控工作池属于窗口级共享设施。
- 用户可见文案不得散落硬编码；日期、数量和文件大小通过统一格式化入口生成。

## 实施切片

### F0：请求身份与过期结果隔离

- [x] 引入 `TabId`、`RequestId` 和单标签 `TabSession`。
- [x] 删除 UI 回调中的同步路径探测，所有验证进入后台任务。
- [x] 后台结果带请求身份；新导航发出后，旧结果不能覆盖当前页面。
- [x] 失败导航保留上次成功列表和路径，并展示可恢复错误。
- [x] 为请求代次、过期结果和失败保留旧状态增加测试。

验收：连续提交快、慢、无效路径时，窗口保持响应，最终只显示最后一次有效请求的结果。

### F1：无损路径与稳定条目 ID

- [x] `FileEntry` 保存原始文件名和原始路径；展示名称单独生成。
- [x] Slint 行模型删除可执行的字符串路径，改用稳定 `EntryId`。
- [x] 打开、重命名和后续文件操作通过 ID 找回原始路径。
- [x] 覆盖简繁中文、日文、韩文、Emoji、组合字符和不能无损转成 UTF-8 的 Windows 名称。

验收：展示替代字符不会改变真实操作目标；显示文本不能被当作文件身份。

### F2：可取消的分批加载

- [x] 目录任务支持取消，并在枚举和元数据读取之间检查取消状态。
- [x] 条目按批次提交，完成时再提交最终状态；排序策略不得阻塞 UI。
- [x] 使用受控工作池，单个慢位置不能堵住全部标签页。
- [x] 定义加载中、部分结果、完成、取消、权限不足、断开和普通失败状态。
- [x] 记录首批结果时间、取消延迟和被丢弃的过期结果。

验收：UNC/断开映射盘或人工慢任务不会冻结 UI，也不会阻塞其他标签页加载。

### F3：多标签页

- [x] 窗口维护标签顺序、活动标签和关闭恢复栈。
- [x] 新建、切换、关闭和恢复标签；每个标签保留独立历史和页面状态。
- [x] 切换时立即显示该标签上次成功内容，后台刷新不影响其他标签。
- [x] 会话恢复只保存稳定、明确的数据，不序列化运行中的任务。

验收：任一标签加载、失败或断开时，其他标签仍可正常浏览。

### F4：本地化基础

- [x] 建立语言资源入口，先提供简体中文和英文。
- [x] 迁移按钮、列名、状态、错误、文件类型和相对时间等所有用户文案。
- [x] 定义缺失翻译行为；增加资源完整性检查。
- [x] 验证 Windows UI 正文字体及中文回退，图标字体不得承担正文；P1 负责迁移到统一字体样式并做全页面与高 DPI 检查。

验收：运行时切换中英文后界面文案完整，中文和混合文字不乱码、不截断关键操作。

## 网络功能接入条件

只有 F0–F3 完成后才开始网络协议。接入时每个提供者必须实现同一组行为语义：连接超时、读取超时、取消、分批结果、错误分类、限并发、重试上限和断线恢复。协议实现不得进入 UI，也不得把底层阻塞调用放进 UI 线程。

## P2 文件操作接入边界

P2 的完整设计见 [P2 基础文件操作与右键菜单实施方案](p2-file-operations-plan.md)。文件任务中心属于窗口级共享设施，使用独立 `OperationId` 和工作池，不复用标签导航的 `RequestId`，也不得占用目录读取或 Shell 图标线程。UI 只提交 `TabId + EntryId` 等意图；应用协调层在提交时解析为 Rust 原始路径快照，后台操作与 Windows Shell/COM 继续放在各自独立模块。

文件操作完成后发出基于原始路径的变化事件，应用协调层更新所有显示源目录或目标目录的标签；无法安全局部投影时，只为受影响标签生成新的目录 `RequestId` 并定向刷新。复制、移动、回收站删除和永久删除的取消只停止未开始工作，不回滚已经完成的项目；界面必须准确显示部分完成。右键菜单是 P2 的主要功能入口，基础文件命令进入 AsterFiles 任务中心，其他 Shell 命令通过 Windows 执行，第三方扩展隔离和超时强化留到 P3。

## P3 Windows 拖放接入边界

双向文件拖放的完整设计见 [P3 Windows 文件拖放实施方案](p3-windows-drag-drop-plan.md)。拖入 AsterFiles 的真实文件系统对象只产生用户意图和原始路径快照，复制、移动、冲突、取消和进度继续由 P2 窗口级 `OperationManager` 执行；不得建立第二套文件操作引擎。拖出到 Explorer 时，AsterFiles 只提供标准 Shell 数据对象和允许效果，最终复制、移动或创建快捷方式由目标应用执行。

OLE 的 `IDataObject`、`IDropSource`、`IDropTarget`、窗口注册和拖放循环集中在 `platform/windows/drag_drop`。COM 对象遵守 STA 与窗口线程亲和，UI 不枚举目录、读取元数据或执行磁盘操作。拖动开始时由应用协调层把 `TabId + EntryId` 固定为原始路径快照；拖入任务沿用独立 `OperationId`，拖放不得复用导航 `RequestId`。操作后的路径变化继续广播给所有相关标签，外部目标无法返回可靠结果时只定向刷新，不猜测或伪造移动成功。

## 验证与产物

- 统一检查：`python tools/verify.py`，覆盖格式、静态检查、测试、确定性无界面场景和 Debug 构建；正式 Release 由用户明确要求后通过 `python tools/verify.py --release` 触发。
- 单元测试覆盖请求代次、标签隔离、路径身份和取消状态转换。
- 机器可读汇总写入 `artifacts/verify/summary.json`；UI 状态写入 `artifacts/state/`；截图写入 `artifacts/ui/`；加载与取消日志写入 `artifacts/logs/`；性能数据写入 `artifacts/perf/`。
- Agent 调试入口和状态字段见 [agent/debug-validation.md](agent/debug-validation.md)。
- 每完成一个切片，同步更新本文和 `docs/task-list.md`，不保留已经失效的设计。

## 当前完成状态（2026-08-28）

F0–F4 已完成首个端到端版本：路径操作通过稳定条目 ID 回到 Rust 原始路径；目录读取使用 4 个受控工作线程、64 项一批提交并支持取消；每个标签拥有独立路径、历史、列表、加载状态和错误；支持标签新建、切换、关闭、恢复及稳定路径会话恢复；简体中文和英文文案通过统一入口生成。

目录激活现已覆盖 Windows 路径快捷方式：后台枚举会跟随目录符号链接和 junction 判断目标类型，并解析 `.lnk` 的目录目标；这些目录统一复用标签内导航和现有错误页，文件快捷方式仍交给 Windows Shell，以保留启动参数和默认应用语义。解析和磁盘判断继续留在目录工作线程，不进入 UI 线程。
当前已在基础能力之上完成 P0 导航与地址输入闭环：每个标签独立支持后退、前进、向上、刷新和右键历史跳转；成功历史只在加载成功后提交，失败目标保留为当前可见错误位置；同路径不会重复加载；快速导航依靠 `TabId + RequestId` 取消并丢弃迟到结果。地址栏默认显示当前可见位置，`Ctrl+L` / `Alt+D` 进入编辑并全选，`Enter` 提交，`Escape` 返回；失败页保留目标路径与面包屑、隐藏上个目录列表并区分不存在、权限、断开与普通读取失败；权限页只提供交给 Windows Explorer 请求访问的入口，后退与刷新复用顶部导航。导航、标签、侧栏和文件列表已接入有许可记录的 Fluent SVG 基础图标。P0 浏览闭环现已完成：面包屑节点直接导航且只在地址栏空白处进入编辑；编辑态保持地址栏浅色外观，并可通过取消按钮、`Escape` 或点击栏外恢复面包屑；文件列表整行支持双击打开，侧栏作为快捷入口仅提供点击和悬停反馈，不与当前路径绑定；文件/目录打开、选择、排序、完整页面状态、真实系统侧栏、响应式命令区与窗口/标签会话恢复均已接入；后续按 `task-list.md` 进入 P1 视觉系统与 Shell 文件图标。
P1 的设置边界已经确定：标题栏左侧菜单进入单例设置标签，页面采用左侧分类、右侧设置内容的双栏布局；主题支持跟随系统、浅色和深色，其中跟随系统只在启动时读取，用户选择可以覆盖并持久化；语言切换从侧栏迁入设置页。设置标签具有明确的非路径类型，不参与目录导航、后台目录与 Shell 图标任务、关闭标签恢复或跨重启标签恢复。主题和语言仍是窗口级共享设置，不进入单个标签的导航状态。高对比度和系统主题实时监听后置。Shell 图标完整归 P1，仍须遵守后台执行、标签与请求身份隔离、取消和迟到结果丢弃边界。

P1 已完成：窗口使用统一视觉样式入口，标题栏菜单进入包含主题与语言的单例设置标签，设置页面采用分类与内容双栏布局，两项设置随窗口会话持久化；主题覆盖标题栏、导航、侧栏、列表、状态页与设置页。静态 Fluent SVG 通过语义映射集中引用，文件列表和系统侧栏先显示通用图标，再由独立 Windows Shell 后台模块取得真实图标并原位替换。图标任务使用独立受控工作线程；新导航会跳过旧请求队列，迟到结果还会按 `TabId + RequestId + EntryId + 原始路径` 再次核对，不能写入新页面。

标签壳层已按 [custom-titlebar-basic.md](custom-titlebar-basic.md) 完成自定义标题栏基础版：标签关闭继续通过稳定 `TabId` 定位并取消对应后台任务，关闭非活动标签不会改变当前会话；Windows 原生贴靠布局等非客户区深度集成继续后置，不改变现有标签、路径与请求身份边界。激活标签、地址栏背景区与侧栏现统一采用 `#323437` 无轮廓壳层；该调整只改变展示，标签身份、地址输入和后台导航边界保持不变。地址输入框独立保留自身背景但不显示描边，次要导航按钮折叠时彻底退出布局并将空间交给地址框；文件列表表头与行状态采用无描边表达，四个详情列始终保留，右侧滚动条避开窗口缩放命中区。

标签与目录展示性能已收紧：新建标签先创建壳层；活动标签已完成加载时共享 Arc 只读目录快照，不重新枚举目录，后续导航、刷新和排序通过写时复制继续保持标签独立。标签栏按窗口实时可用宽度收缩至 80px，随后仅滚动和裁切展示，并固定预留新建按钮、窗口控制与拖动区域；活动标签的稳定 `TabId` 与顺序仍由 Rust 管理，UI 只接收活动索引用于保持其可见，溢出菜单不复制或改变标签状态。打开新路径仍使用 TabId + RequestId 独立加载，批次通过 Slint 模型增量追加，完成时只做一次排序后的完整投影。文件列表的 UI 投影也记录当前 `TabId + RequestId`；导航、刷新或切换标签后先重置滚动位置，再投影新模型，增量追加发现投影身份或模型类型不匹配时立即按 Rust 当前状态完整重建，防止状态栏已统计完整条目而列表仍停留在旧的部分模型。Shell 图标继续在独立工作线程读取，缓存命中不重复调用 Shell，未命中先显示通用占位图标；返回结果按 TabId + RequestId + EntryId + 原始路径 校验并只更新对应行。
## P2 当前实施状态（2026-08-29）

P2 已接入独立的窗口级单写文件任务队列，使用 `OperationId`、单向状态和原始 `PathBuf` 快照；目录读取、Shell 图标和文件写任务继续使用分离工作线程。核心操作覆盖名称校验、仅大小写重命名、复制、跨卷移动、目录合并、永久删除与同卷快速移除，递归处理不跟随链接。

Windows 适配集中在 `platform/windows`：文件剪贴板使用 `CF_HDROP + Preferred DropEffect` 与 Explorer 互通，回收站删除失败不会升级为永久删除；经典 Shell 菜单保持 COM/HMENU 同线程生命周期，基础剪切、复制、粘贴、重命名和删除仍由 AsterFiles 接管。操作完成只对显示受影响原始目录的标签生成新 `RequestId` 并刷新。

右键菜单、标准快捷键、永久删除确认、任务中心和活动任务退出确认已接线。永久删除、文件冲突和退出确认使用三个独立、固定尺寸、不可缩放的 Slint 窗口，复用进度窗口的单层纯色标题栏与操作按钮；窗口按主窗口或可见进度窗口所在显示器居中、限制在屏幕范围内并置于前方，同一实例可见时只聚焦而不重复创建。标题栏关闭承担安全返回：取消永久删除、跳过当前冲突、等待任务；冲突窗口保存准确 `OperationId`，用户决定仍可按类别作用于当前任务。复制/移动按块上报字节，任务中心显示速度、剩余时间、取消、部分完成和失败项重试。大目录判定与清理均留在文件工作线程，回收站使用强制回收标志且失败不降级。经典菜单覆盖背景、单项、目录和多选，动态子菜单与原生出口保持 STA 同线程生命周期；剪贴板采用 OLE IDataObject 与 Explorer 共享 CF_HDROP 和 Preferred DropEffect。

长文件操作使用共享 `OperationManager` 的独立 Slint 进度窗口，主窗口状态栏只保留任务入口；窗口本身不拥有或执行文件任务，关闭时仅隐藏。活动任务持续约 0.8 秒后自动显示窗口。窗口采用单层纯色结构，按操作类型显示实时百分比、当前文件、已处理/总文件数、容量、速度和剩余时间；暂停会在下一个安全读写边界阻塞后台任务，继续后从当前位置恢复且暂停时间不计入速度估算。取消立即隐藏窗口并停止任务，成功和完全取消自动清理，失败与部分完成保留重试，不再提供“清除已完成”。

进度窗口使用固定逻辑尺寸且禁止用户缩放，不创建滚动列表；每次自动显示时根据主窗口当前显示器定位并限制在该屏幕范围内。容量、速度、格式化剩余时间位于同一行，暂停时速度只显示一次状态且剩余时间为破折号；进度、按钮按下和悬停颜色复用文件列表的选择与悬停视觉入口。

复制目录在后台成功创建真实目标根目录后立即发出一次创建事件；协调层只刷新正在显示目标父目录的标签，并以最终原始路径聚焦、选中该目录。冲突选择“保留两者”继续使用 Explorer 风格的 `Name (2)`、`Name (3)` 递增名称，不创建 UI 占位项，也不等待整个复制任务完成后才显示成果。

自动验证已覆盖 83 项测试、无界面真实 OperationManager 场景和 Release 构建。Windows 11 的 Explorer 双向互通、UNC、回收站策略及第三方扩展仍需按 [p2-manual-acceptance.md](p2-manual-acceptance.md) 人工留证，不能用单元测试代替。第三方 Shell 扩展的进程隔离与超时继续按 P3 边界后置。
