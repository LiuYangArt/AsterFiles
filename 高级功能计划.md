# 高级功能可行性评估

## 结论

六项需求均可实现，但不应同时开发。当前项目已有后台任务、取消、原始路径和标签隔离基础；仍缺少文件操作任务中心、网络协议、索引适配、缓存和完整的 Windows Shell 适配层。

建议依次完成：本地复制任务中心 → 经典系统右键菜单 → Everything 搜索 → 本地文件夹大小 → 单个网络协议。把 AsterFiles 注册为文件夹打开程序可随 Windows 集成一并实施，不做桌面、任务栏和系统 Shell 的完整替换。

## 1. 文件搜索：集成 Everything

**可行性：高。**

采用 Everything 官方 SDK/IPC，不移植索引核心。Everything 需要在后台运行；AsterFiles 负责提交搜索、接收结果，并继续用原始路径或稳定 ID 执行操作。

设置页面提供 Everything 配置：

- 默认自动发现已安装和正在运行的 Everything；
- 允许用户指定 Everything 可执行文件路径，兼容便携版和非标准安装目录；
- 如 SDK DLL 随 AsterFiles 分发，则由应用固定管理 DLL，不要求用户寻找；若选择外部 DLL，也允许单独覆盖路径；
- 支持测试连接、显示版本与运行状态，并可按配置启动 Everything；
- 路径失效、程序未启动、位数或版本不匹配时给出明确错误，不静默退回慢速全盘扫描。

Everything 本身和 SDK 采用宽松许可，可正常集成。首版只覆盖本地索引结果。

## 2. 文件复制

**可行性：本地高，网盘中等。**

先完成普通本地磁盘、跨磁盘和 UNC/SMB 复制：任务队列、进度、速度、取消、冲突处理、失败原因和重试。Windows 文件复制接口可提供进度和取消，但“跨应用重启继续复制”仍需 AsterFiles 保存任务状态并校验已写入内容。

网盘不能统一套用本地文件复制方案。后续应调用各网盘或协议的分块上传、下载接口实现断点续传；缓存只用于明确的远程传输场景，避免本地复制重复占用空间。

项目可以改为 GPL，因此允许评估复用 WinSCP 的 GPL 代码。但改许可证会影响整个项目及分发义务，实施前应确定采用的 GPL 版本、依赖兼容性和源码提供方式。即使许可允许，也优先复用成熟库或独立模块，不直接搬入 WinSCP 的大型 C++ 界面和历史兼容代码。

## 3. 网络访问

**可行性：中，工程量大。**

UNC/SMB 可继续按 Windows 路径处理。SFTP、SCP、FTP、WebDAV、S3 和厂商网盘是不同协议，不建设一次覆盖全部协议的通用框架。

首期只选择一个明确协议，跑通连接、凭据安全存储、目录分批加载、超时、取消、错误分类、重试、断线恢复和文件传输。WinSCP 用于参考连接管理和传输交互；是否复用其代码在 GPL 决策完成后逐模块判断。

## 4. 注册为文件夹打开程序

**可行性：高，范围已收窄。**

只需要让用户选择使用 AsterFiles 打开文件夹，并提供安装、取消注册和恢复入口。注册应由安装程序或明确的设置操作完成，不在普通启动时反复修改系统。

不替换 Explorer 的桌面、任务栏或 Windows 登录 Shell，也不使用面向 kiosk/企业设备的 Shell Launcher。需验证 Windows 10/11、普通文件夹、磁盘根目录和“打开方式”入口；无法被系统允许为默认处理程序时，应准确说明限制。

## 5. 本地文件夹大小

**可行性：高，采用 Everything 索引结果。**

只显示本地位置的文件夹大小，不统计网络位置。参考 Windhawk 的 [Better file sizes in Explorer details](https://windhawk.net/mods/explorer-details-better-file-sizes)，但 AsterFiles 是独立文件管理器，不注入 Explorer，也不挂钩 Windows 内部函数。

采用 Better File Sizes 已验证的核心方案：文件夹大小直接读取 Everything 维护的文件夹大小索引，不在打开目录时现场递归扫描。搜索和文件夹大小共用同一个 Everything 服务适配层、配置、连接状态与生命周期。

推荐调用链：

1. 设置页完成 Everything 自动发现或由用户指定可执行文件、便携版路径及实例；连接测试成功后保存有效配置；
2. 应用按保存的配置启动或连接 Everything，搜索与文件夹大小复用该连接，不再为每次查询依次猜测默认实例和 `1.5a` 实例；
3. 列表首批内容先显示，文件夹大小保持空值或“查询中”；
4. 普通排序按真实可见范围及前后一屏提交，每批最多新增 24 项，重复滚动去重；
5. 大小排序冻结目录身份快照，有界分批获取全部目录终态，期间不改变列表顺序，完成后一次提交和稳定排序；
6. 使用 Everything3 命名管道命令 18（等价于 SDK 的 `GetFolderSizeFromFilename` 能力）按完整路径读取已索引大小，不分发 SDK DLL；
7. 查询按 `TabId + RequestId + 调度代次 + EntryId + 原始路径` 接受，导航、刷新、排序或关闭标签后的旧结果直接丢弃；
8. 不建立跨刷新缓存，也不使用现场递归扫描回退。

首次配置、用户清除配置或已保存连接失效时，才执行实例自动探测；探测结果应回写设置，后续稳定使用同一实例。不得把每次依次尝试多个实例作为正常查询路径。

源码中的关键经验：

- 文件夹大小来自 Everything 索引，不是现场递归扫描，因此单次查询很快；
- 返回 `-1` 代表没有索引，零值还需区分真正空目录与重解析点；
- 对本地重解析点或未索引路径，可解析为真实路径后只重试一次；
- IPC 必须设置超时并放在后台工作线程，不能阻塞 UI；
- 连接应长时间复用，不为列表中的每个文件夹重新连接或启动 Everything；批量与并发策略需用原型测量后确定。

首版采用 **Everything-only**：Everything 未运行、文件夹大小索引未启用、路径未收录或查询超时时，显示“不可用/未索引”，不自动递归扫描磁盘。这样可以保持行为可预测，也避免打开大目录时突然产生大量磁盘访问。设置页应提供连接测试，并说明需要 Everything 1.5 及文件夹大小索引。

Better File Sizes 中不应照搬的代码包括：注入 Explorer、挂钩 `windows.storage.dll` 内部符号、拦截 `CFSFolder::_GetSize`、隐藏消息窗口的旧 IPC 兼容层，以及 Shell 的递归计算回退。这些都不是 AsterFiles 获取列表大小所必需的，并会增加系统版本兼容风险。

## 6. 经典菜单数据源与 AsterFiles 外壳

**可行性：中高，采用 File Pilot 类似方案。**

Windows 经典右键菜单作为真实命令数据源：通过 Shell `IContextMenu::QueryContextMenu` 获取系统及第三方扩展生成的菜单项、命令编号、层级、分隔线、状态和图标；AsterFiles 使用 Slint 重新绘制外壳，提供统一主题、命令搜索和收藏。用户点击后通过原始 `IContextMenu::InvokeCommand` 执行，避免为 NanaZip、Git、TortoiseSVN、PowerToys 等扩展逐个适配。

实现边界：

- 底层原生菜单和 `IContextMenu` 对象必须保持存活，直到自绘菜单关闭；
- 子菜单按需展开，并转发 `WM_INITMENUPOPUP`；支持 `IContextMenu2/3` 时还需处理测量、绘制和菜单字符消息；
- 常规文字、图标及普通子菜单由 AsterFiles 呈现；无法可靠解析的动态或自绘项目进入原生兼容出口；
- 提供“显示完整经典菜单”，确保特殊扩展仍能通过 Windows 原生菜单使用；
- 第一版依次支持单个文件、单个文件夹、目录空白处，最后扩展多选；
- Shell/COM 工作集中在 `platform/windows`，菜单模型只向 UI 暴露稳定 ID 和展示信息。

主要风险是第三方扩展可能缓慢、崩溃，或者依赖真实菜单窗口与自绘消息。首个原型必须验证动态子菜单、图标、管理员命令、属性、发送到、压缩软件和版本控制扩展；不能承诺所有扩展都能完整进入 Slint 外壳，原生兼容出口是正式设计的一部分。

## 推荐实施顺序

1. 本地复制任务中心及冲突处理；
2. 经典菜单数据源与 AsterFiles 自绘外壳原型；
3. Everything 搜索与设置页路径配置；
4. 使用 Everything 索引显示本地文件夹大小；
5. 选择一个网络协议完成端到端版本；
6. 在安装与卸载流程中加入文件夹打开程序注册。

## 开始实施前需确认

- 最低支持的 Windows 版本；
- AsterFiles 改用的具体 GPL 版本及发布时间点；
- 首个网络协议或具体网盘；
- Everything 是用户自备，还是安装包可选捆绑；
- 文件夹打开注册需要覆盖哪些 Windows 入口。
